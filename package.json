{
  "name": "wordpress-monorepo",
  "private": true,
  "workspaces": [
    "wordpress-nginx",
    "wordpress-php-fpm"
  ],
  "scripts": {
    "build": "docker compose build",
    "start": "docker compose up --force-recreate --remove-orphans --build",
    "stop": "docker compose down"
  }
}