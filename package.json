{
  "name": "@mwaeckerlin/wordpress",
  "private": true,
  "workspaces": [
    "nginx",
    "php-fpm"
  ],
  "scripts": {
    "build": "docker compose build",
    "start": "docker compose up -d --force-recreate --remove-orphans --build",
    "stop": "docker compose down"
  }
}
